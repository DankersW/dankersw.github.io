import React from "react";
import "./contact.css"

export default function Contact() {
    return (
        <div className="contact-container">
            <h1 className="section-title">Contact</h1>
            <h2 className="sub-title">Let's become internet BFF's</h2>
        </div>
    );
}